# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Project activity log

Before starting work, read `activity_log.md` in this same folder — it's a dated log of every past code change AND Google Play Console/AdMob dashboard action, so you know what's already been done without asking the user to re-explain. After completing a meaningful step (a feature, a fix, a dashboard action the user reports), append a short dated entry to it.
